# 参与 Douyin-EchoCue 贡献

[English](CONTRIBUTING.md) | [简体中文](CONTRIBUTING_zh.md)

感谢贡献。本项目接受 AI 辅助编码，但每位贡献者都要对最终代码质量、验证结果和可审查性负责。

## 必要流程

1. 先 fork 仓库。
2. 克隆自己的 fork，并创建独立分支。
3. 在聚焦的范围内完成修改。
4. 在发起 PR 前完成必要检查。
5. 从 fork 向上游仓库提交 pull request。

不建议直接从上游仓库上的分支发起 PR。

## PR 前必须满足的条件

只有在修改已经准备好接受审查，并且满足以下条件时，才允许打开 PR：

- 变更已经完成，并且范围清晰。
- 代码已完成格式化，且没有 lint 问题。
- 相关测试已通过。
- 如果改动涉及共享抽象、数据库行为、响应结构或其他类型边界，需要通过类型检查。
- 如果改动明显影响共享逻辑或高风险行为，需要更新或复核覆盖率。
- 不包含密钥、本地专用配置或无关工作内容。
- PR 描述中说明了改动内容和已执行的验证。

## 建议的验证

- 仅文档变更：在可行时运行 `make check`。
- 普通代码变更：在可行时运行 `make test` 和 `make lint`。
- 涉及类型、共享抽象、数据库或响应结构的变更：在可行时运行 `make type-check` 和 `make coverage`。
- 范围更大或风险更高的变更：在打开 PR 前，针对受影响部分运行可行的最高强度校验。

## 分支与提交要求

- 分支范围要尽量收窄。
- 条件允许时使用原子提交。
- 不要把无关重构、格式化和功能修改混在同一个 PR 里。

## AI 辅助编码

允许使用 AI 辅助编码。

如果使用了 AI 工具：

- 需要自己复核生成代码，
- 需要执行相关检查，
- 需要确保最终 PR 体现的是自己的审查和判断。
