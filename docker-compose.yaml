name: echocue

services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: echocue
      POSTGRES_USER: echocue
      POSTGRES_PASSWORD: echocue
    expose:
      - "5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U echocue -d echocue"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7.2-alpine
    expose:
      - "6379"
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10

  qdrant:
    image: qdrant/qdrant:latest
    expose:
      - "6333"
      - "6334"
    volumes:
      - qdrant-data:/qdrant/storage

  live:
    image: ghcr.io/jwwsjlm/douyinlive:v2.0.25
    command: ["--config", "/app/config.yaml"]
    expose:
      - "1088"
    volumes:
      - ./config/live.config.yaml:/app/config.yaml:ro

  lexicon:
    image: echocue:0.1.0
    command: ["uv", "run", "app", "lexicon", "serve"]
    expose:
      - "50051"
    depends_on:
      qdrant:
        condition: service_started
    volumes:
      - ./config/app.config.yaml:/app/config.yaml:ro
      - ./assets:/app/assets:ro

  auth:
    image: echocue:0.1.0
    command: ["uv", "run", "app", "auth", "serve"]
    expose:
      - "50052"
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ./config/app.config.yaml:/app/config.yaml:ro
      - ./logs:/app/logs

  app:
    image: echocue:0.1.0
    build:
      context: .
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      qdrant:
        condition: service_started
      live:
        condition: service_started
      lexicon:
        condition: service_started
      auth:
        condition: service_started
    volumes:
      - ./config/app.config.yaml:/app/config.yaml:ro
      - ./logs:/app/logs

volumes:
  postgres-data:
  redis-data:
  qdrant-data:
